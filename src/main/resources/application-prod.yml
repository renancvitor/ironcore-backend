spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: false

messaging:
    provider: none
    kafka:
      enabled: false

outbox:
   scheduler:
     enabled: false
